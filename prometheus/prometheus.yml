global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["prometheus:9090"]

  # Add application metrics targets here once services expose /metrics.
  # Example:
  # - job_name: pm-trades-db
  #   static_configs:
  #     - targets: ["host.docker.internal:8001"]
